const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres:Out2021adm.@db.inmyfrdeiqbzcavijnkg.supabase.co:6543/postgres', ssl: { rejectUnauthorized: false } }); pool.query('SELECT id, name, role, pin_code FROM app_users WHERE name ILIKE \ AND active = true', ['Administrador']).then(res => console.log(res.rows)).catch(console.error).finally(() => pool.end());
